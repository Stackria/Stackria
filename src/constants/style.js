import { museo, outfitLight, outfitRegular, museoReg } from '@/app/layout'

export const styles = {
    heading1: `${museoReg.className} text-secondary text-center  text-[36px]`,
    headingLeft: `${museoReg.className} text-secondary text-left  text-[15px]`,
    paragraph: `${outfitLight.className} text-center text-customText text-[15px]`,
    paragraphDetails: `${outfitRegular.className} text-left text-slate-500 text-xs font-bold`,
    flexCenter: "flex justify-center items-center",
    flexStart: "flex justify-center items-start",
}

export const layout = {

}