import { museo, outfitLight, outfitRegular, museoReg, outfitSemibold } from '@/app/layout'

export const styles = {
    heading1: `${outfitRegular.className} text-secondary text-center leading-7 lg:leading-10 text-[36px]`,
    buttonLeft: `${outfitLight.className} text-black text-left  text-[15px] xl:text-[25px]`,
    paragraph: `${outfitLight.className} text-center xl:text-left text-customText text-[15px]`,
    paragraphDetails: `${outfitRegular.className} text-left text-slate-500 text-[15px] xl:text-[16px] font-semi-bold text-justify`,
    flexCenter: "flex justify-center items-center",
    flexStart: "flex justify-center items-start",
}

export const layout = {

}