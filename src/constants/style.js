import { museo, outfitLight, outfitRegular, museoReg } from '@/app/layout'

export const styles = {
    heading1: `${museoReg.className} text-secondary text-center  text-[36px]`,
    headingLeft: `${museoReg.className} text-black text-left  text-[15px] xl:text-[25px]`,
    paragraph: `${outfitLight.className} text-center xl:text-left text-customText text-[15px]`,
    paragraphDetails: `${outfitRegular.className} text-left text-slate-500 text-sm xl:text-[16px] font-bold`,
    flexCenter: "flex justify-center items-center",
    flexStart: "flex justify-center items-start",
}

export const layout = {

}